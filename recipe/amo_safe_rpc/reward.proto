syntax = "proto3";
package reward;

service RewardService {
  rpc Score (ScoreRequest) returns (ScoreResponse) {}
}

message ScoreRequest {
  string prompt = 1;
  string response = 2;
}

message ScoreResponse {
  double helpful_score = 1;
}