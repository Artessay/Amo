syntax = "proto3";
package recipe.amo_safe;

service RewardService {
  rpc ComputeScore (ScoreRequest) returns (ScoreResponse) {}
}

message ScoreRequest {
  string prompt = 1;
  string response = 2;
}

message ScoreResponse {
  double reward_score = 1;
}